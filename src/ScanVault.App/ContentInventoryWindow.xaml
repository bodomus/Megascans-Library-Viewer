<Window x:Class="ScanVault.App.ContentInventoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Content inventory" Width="900" Height="680" MinWidth="700" MinHeight="500"
        WindowStartupLocation="CenterOwner" Background="{StaticResource WindowBackground}"
        Foreground="{StaticResource TextPrimary}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="SemiBold" />
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding AssetId, StringFormat=ID: {0}}" Foreground="{StaticResource TextSecondary}" />
                <TextBlock Text="{Binding Status, StringFormat=  Status: {0}}" Foreground="{StaticResource Accent}" />
            </StackPanel>
        </StackPanel>
        <TabControl Grid.Row="1" Margin="0,12,0,12">
            <TabItem Header="Variants and LODs">
                <ListBox ItemsSource="{Binding VariantLines}" Background="{StaticResource PanelBackground}" Foreground="{StaticResource TextPrimary}" />
            </TabItem>
            <TabItem Header="Texture sets">
                <ListBox ItemsSource="{Binding TextureSetLines}" Background="{StaticResource PanelBackground}" Foreground="{StaticResource TextPrimary}" />
            </TabItem>
            <TabItem Header="Files">
                <DataGrid ItemsSource="{Binding Files}" AutoGenerateColumns="False" IsReadOnly="True"
                          Background="{StaticResource PanelBackground}" Foreground="{StaticResource TextPrimary}"
                          HeadersVisibility="Column" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*" />
                        <DataGridTemplateColumn Header="Actions" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Copy" Command="{Binding CopyPathCommand}" Margin="0,0,6,0" Padding="8,2" />
                                        <Button Content="Open folder" Command="{Binding OpenContainingFolderCommand}" Padding="8,2" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Issues">
                <Grid Background="{StaticResource PanelBackground}">
                    <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <GroupBox Header="Issues" Margin="6"><ListBox ItemsSource="{Binding IssueLines}" /></GroupBox>
                    <GroupBox Grid.Row="1" Header="Unclassified files" Margin="6"><ListBox ItemsSource="{Binding UnclassifiedLines}" /></GroupBox>
                </Grid>
            </TabItem>
        </TabControl>
        <Button Grid.Row="2" Content="Close" Width="90" Padding="12,5" HorizontalAlignment="Right" IsCancel="True" />
    </Grid>
</Window>
