<Window x:Class="ScanVault.App.SmartCollectionDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="460" Height="360" MinWidth="420" MinHeight="320"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBackground}"
        Foreground="{StaticResource TextPrimary}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Name" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" Padding="8,5" MaxLength="80" />
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Description" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" />
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" Padding="8,5" Height="64" TextWrapping="Wrap" AcceptsReturn="True" />
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Folder scope" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" />
                <ComboBox ItemsSource="{Binding FolderScopes}" DisplayMemberPath="Label" SelectedValuePath="Scope" SelectedValue="{Binding FolderScope, Mode=TwoWay}" Margin="0,4,14,0" />
            </StackPanel>
            <CheckBox Grid.Column="1" Content="Save sort" IsChecked="{Binding SaveSort}" VerticalAlignment="Bottom" Margin="0,0,0,4" Foreground="{StaticResource TextCheckBox}" />
        </Grid>

        <Border Grid.Row="3" BorderBrush="#343B46" BorderThickness="1" Padding="10" Background="#20252D">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding CriteriaSummary}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondary}" />
            </ScrollViewer>
        </Border>

        <DockPanel Grid.Row="4" Margin="0,14,0,0">
            <Button DockPanel.Dock="Right" Content="Save" Width="86" Padding="12,5" Click="OnSaveClick" IsDefault="True" />
            <Button DockPanel.Dock="Right" Content="Cancel" Width="86" Padding="12,5" Margin="0,0,8,0" IsCancel="True" />
        </DockPanel>
    </Grid>
</Window>
